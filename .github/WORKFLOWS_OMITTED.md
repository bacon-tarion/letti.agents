GitHub Actions workflows from upstream were omitted on first push because the fine-grained PAT lacked Workflows permission.
Re-add Workflows: Read and write on the PAT, then restore from upstream if you want CI.
